---
"ftw": patch
---

The Weather settings tab is gone as a tab: location, forecast source, PV
arrays and roof derivation are inputs to the planner, so they now render
inside the Planner tab, above the engine controls. Nothing about the
fields themselves changed — only where they live.
